// This file is intentionally left as a redirect. The reparameterization
// helper now lives in the parent `ai` module (mod.rs) so that model sub-modules
// can import it via `use crate::reparameterize` without circular dependencies.
//
// See `crates/mitchty/src/ai/mod.rs`.
